name: Daily data refresh

# Runs every weekday at 14:00 UTC (19:30 IST). That is after the Indian market
# close (15:30 IST) and after BSE typically publishes end-of-day announcements.
# Manual trigger also available from the Actions tab.

on:
  schedule:
    - cron: '0 14 * * 1-5'   # Mon-Fri 14:00 UTC
  workflow_dispatch:           # Allow manual runs from the GitHub UI

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: write           # So the bot can commit updated data back
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1         # shallow clone keeps CI fast

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Refresh price data
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: python -m scripts.bootstrap

      - name: Refresh BSE announcements
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: python -m scripts.build_rag

      - name: Refresh FRED macro and NSE FII/DII
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: python -m scripts.refresh_macro

      # Both stores grow every run (DuckDB never releases freed blocks, SQLite
      # leaves free pages). market.duckdb reached 97 MiB on 2026-06-08 and the
      # next run pushed it past GitHub's hard 100 MiB per-file limit, which is
      # why every push since then was rejected. Compacting here keeps it ~13 MiB
      # and fails the build early if that ever stops being enough.
      - name: Compact data files
        run: python -m scripts.compact_data

      - name: Commit refreshed data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/ chroma_db/ || true
          if git diff --cached --quiet; then
            echo "No data changes to commit."
          else
            git commit -m "chore(data): automated daily refresh $(date -u +%Y-%m-%d)"
            # Rebase on top of any commits pushed while we were running
            # (e.g. user edits to README). Then push cleanly.
            git pull --rebase --autostash origin main
            git push
          fi
